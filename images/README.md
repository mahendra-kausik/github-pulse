# Images

The architecture diagram lives as ASCII in the root `README.md` (renders anywhere, never goes
stale). The dashboard screenshots are linked from `README.md`'s Dashboard section:

- `dashboard.png` — page 1: trending repos (bar) + language momentum (time series), with the
  page-level date-range control and the language drop-down.
- `dashboard-momentum.png` — page 2: the momentum-burst table, with the repo search control.

Split across two report pages because the momentum table needs the vertical room to show a
meaningful ranking depth (20 rows of 9,186 repos).
