# --- ingestion ---
requests==2.32.3
pyarrow==16.1.0
google-cloud-storage==2.17.0
google-cloud-bigquery==3.25.0
tzdata; sys_platform == 'win32'    # Windows has no system IANA tz db; needed by pyarrow's zoneinfo lookups

# --- transform ---
dbt-bigquery==1.8.2

# --- dev: lint + test ---
pytest==8.2.2
ruff==0.5.5
sqlfluff==3.1.0
