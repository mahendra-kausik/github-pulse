# --- ingestion ---
requests==2.32.3
pyarrow==16.1.0
ijson==3.3.0                       # streaming JSON parse (avoid loading whole files into RAM)
google-cloud-storage==2.17.0
google-cloud-bigquery==3.25.0

# --- transform ---
dbt-bigquery==1.8.2

# --- dev: lint + test ---
pytest==8.2.2
ruff==0.5.5
sqlfluff==3.1.0
sqlfluff-templater-dbt==3.1.0
