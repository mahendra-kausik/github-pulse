name: github_pulse
version: "0.1.0"
config-version: 2

profile: github_pulse

model-paths: ["models"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  github_pulse:
    staging:
      +materialized: view
      +schema: raw
    marts:
      +materialized: table
      +schema: marts
